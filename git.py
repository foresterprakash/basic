# step 1 : Create a remote repo
# step 2 : Add remote <remote> to local
# step 3 : git push -v <name> main

# Git branch 
# 1. git branch # List all the branches and highlights the currrent branch
# 2. git branch <name> #creates a new branch
# 3. git checkout<branch_name> # check out to new branch


    # step 2 and 3 can be obtained form sngle command from the below command
    # git checkout -b <branch_name>

#4. git merge <branch_name> # This merges the code from branch name to current branch the current branch 

